<div class="card-trip">
  <div class="card">
  <%= cl_image_tag pet.photo.key, width: 300 %>
    <div class="card-trip-infos">
      <div>
        <h2><%= pet.name %></h2>
        <p><%= pet.species %></p>
      <h2 class="card-trip-pricing"><%= pet.price %>€</h2>
      </div>
    <div class="card-trip-user avatar-bordered" > <i class="fa-solid fa-star" style="color: #000000;"></i><%= pet.rating %> </div>
    </div>
  </div>
</div>

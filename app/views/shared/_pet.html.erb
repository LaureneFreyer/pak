<div class="card-trip">
  <div class="card">
  <%= cl_image_tag pet.photo.key, width: 100, height: 150 %>
  <div class="card-trip-user" > <i class="fa-solid fa-paw" style="color: white;"></i><%= pet.rating %> </div>
    <div class="card-trip-infos">
      <div >
        <h2><%= link_to pet.name, pet_path(pet), class:'link-pet-name' %></h2>
        <p><%= pet.species %></p>
        <div class="city">
          <p><i class="fa-solid fa-location-dot"></i> <%= pet.address %></p>
          </div>
      <h3 class="card-trip-pricing"><%= pet.price %>€</h3>
      </div>
    </div>
  </div>
</div>

<div class="card-trip">
  <div class="card">
  <%= cl_image_tag pet.photo.key, width: 100, height: 150 %>
    <div class="card-trip-infos">
      <div>
        <h2><%= pet.name %></h2>
        <p><%= pet.species %></p>
      </div>
      <h2 class="card-trip-pricing"><%= pet.price %></h2>.€
    <div class="card-trip-user avatar-bordered" > <%= pet.rating %> </div>
    </div>
  </div>
</div>
